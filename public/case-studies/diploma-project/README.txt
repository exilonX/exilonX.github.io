Images expected in this folder (referenced by src/pages/CaseStudyDiploma.tsx):

  people-graph.png
      The force-atlas layout of the business-persons graph.
      The black-on-white spider-ball image you attached in chat.

  companies-graph.png
      The force-atlas layout of the companies graph — slide 7 of the defense
      deck. Looks similar to the person graph but visibly denser.

Note: the pipeline diagram is NOT a PNG — it's rendered inline as an SVG
component in CaseStudyDiploma.tsx (PipelineDiagram). No file needed for it.

Files should be PNG and ideally 1200–1600 px wide. White or transparent
background is fine — the case study wraps them in a white panel so dark-theme
readers can still see the original black-on-white graph renders.
